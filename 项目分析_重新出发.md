# SQLBot 项目分析 — 重新出发

> 本文档用于在代码基本定版后，重新梳理项目架构与关键流程，便于后续维护与扩展时快速恢复上下文。

---

## 一、项目定位

**SQLBot** 是一个**智能问数助手**：用户用自然语言提问 → 系统自动生成 SQL → 执行并返回数据 → 可选生成图表/分析/预测。支持多数据源、多轮对话、术语与训练数据增强、行/列权限等。

---

## 二、技术栈一览

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端框架 | FastAPI | Python 3.11+ |
| 后端数据库 | PostgreSQL | 业务库；也支持 MySQL/SQL Server/Oracle/Excel 等作为**数据源** |
| ORM | SQLModel | 与 SQLAlchemy 兼容 |
| AI/LLM | LangChain + 多模型 | OpenAI、国产大模型等，通过 `LLMFactory` 统一 |
| 向量/检索 | pgvector + embedding | 术语、训练数据、表结构检索 |
| 缓存 | Redis / 内存 | `common.core.sqlbot_cache` |
| 前端 | Vue 3 + TypeScript + Vite | |
| UI | Element Plus | |
| 图表 | AntV G2、AntV S2 | 另有 g2-ssr 做服务端渲染 |
| 扩展 | sqlbot_xpack | 许可证、自定义提示词等 |

---

## 三、目录与模块结构

### 3.1 根目录

```
.
├── backend/          # 后端 (FastAPI)
├── frontend/         # 前端 (Vue 3)
├── g2-ssr/           # G2 图表服务端渲染
├── README.md
├── project_analysis.md   # 原有分析（优缺点与优化建议）
└── 项目分析_重新出发.md  # 本文档
```

### 3.2 后端 (backend)

- **main.py**  
  - FastAPI 入口；`lifespan` 内：迁移、缓存、术语/训练 embedding 初始化、xpack 清理、模型信息异步加密。  
  - 中间件：CORS、TokenMiddleware、ResponseMiddleware。  
  - 路由挂载在 `api_router`，前缀 `API_V1_STR`（如 `/api/v1`）。  
  - 集成 **FastApiMCP**（MCP Server）、**sqlbot_xpack**。

- **apps/api.py**  
  总路由聚合：login、callback、oauth2、auth、user、workspace、assistant、aimodel、terminology、custom_prompt、data_training、datasource、**chat**、dashboard_api、mcp、table_relation。

- **apps/** 业务模块（按领域划分）  
  - **chat**：对话与问答核心（见下）。  
  - **ai_model**：LLM/Embedding 工厂（model_factory、openai 等）。  
  - **datasource**：数据源 CRUD、表/字段、权限（行/列）、embedding、table_relation。  
  - **template**：各类提示词模板（generate_sql、generate_chart、generate_analysis、generate_predict、question_enhance、select_datasource、filter、generate_dynamic、generate_guess_question）。  
  - **terminology**：业务术语与同义词。  
  - **data_training**：训练数据（问句-SQL 示例）。  
  - **dashboard**：仪表盘/大屏。  
  - **system**：用户、工作区、助手、AI 模型配置、登录/OAuth2、custom_prompt 等。  
  - **settings**：术语等配置 API。  
  - **mcp**：MCP 相关接口。  
  - **db**：多数据源执行引擎（exec_sql、get_version、check_connection 等）。

- **common/**  
  - **core**：config、db（engine）、deps（CurrentUser/SessionDep/CurrentAssistant）、security、sqlbot_cache、response_middleware。  
  - **utils**：加解密、embedding 填充、日志等。  
  - **error**：SingleMessageError、SQLBotDBError 等。

### 3.3 前端 (frontend/src)

- **入口**：main.ts → App.vue，路由 `router/index.ts`（createWebHashHistory）。
- **路由要点**：  
  - `/login`、`/home`、`/oauth2/callback` 独立页。  
  - `/chat`：LayoutDsl → 数据问答主界面（chat/index）。  
  - `/ds`：数据连接管理；`/dsTable/:dsId/:dsName` 表列表。  
  - `/dashboard`：仪表盘列表；`/canvas` 编辑、`/dashboard-preview` 预览。  
  - `/set/*`：工作区内设置（成员、权限、专业术语、训练、提示词、热门问题等）。  
  - `/system/*`：系统级（用户、工作区、模型、嵌入式、外观等）。  
  - `/assistant`、`/embeddedPage`、`/assistantTest`：助手/嵌入页。  
  - `/401`、`/403`、`/404` 及兜底 404。

- **api/**：对后端 REST 的封装（auth、chat、datasource、dashboard、workspace、assistant、training、prompt 等）。
- **stores/**：Pinia（user、assistant、appearance、dashboard 等）。
- **views/**：按功能分 chat、dashboard、ds、system、embedded、login、oauth2、error、home 等。
- **components/**：布局（layout、LayoutDsl、SinglePage）、抽屉、语言选择、富文本等。

---

## 四、核心流程：一问一答（Chat → LLM → SQL → 图表）

### 4.1 请求入口

- 前端：`questionApi.add(data, controller)` → `request.fetchStream('/chat/question', data, controller)`。  
- 后端：`apps/chat/api/chat.py` 中 `POST /question` → `stream_sql()`。

### 4.2 后端处理链路（简要）

1. **创建 LLM 服务**  
   `LLMService.create(current_user, request_question, current_assistant, embedding=True)`  
   - 读取默认 LLM 配置（get_default_config）。  
   - 根据 `chat_id` 取 Chat，校验创建人权限；再取关联数据源（CoreDatasource 或 Assistant 动态数据源）。  
   - 若为普通数据源：取表结构（get_table_schema，含 embedding 检索）；若为助手动态数据源：走 AssistantOutDs。  
   - 加载该会话的 generate_sql_logs / generate_chart_logs；若尚无记录则 `change_title=True`（后续可自动改会话标题）。  
   - 构造 LLM 实例（LLMFactory.create_llm）、最后一条执行错误信息（若有）写入 `chat_question.error_msg`。

2. **执行任务并流式返回**  
   - `llm_service.run_task_async(in_chat=True, stream=True, finish_step=...)` 将 `run_task_cache` 丢到线程池执行。  
   - 接口通过 `StreamingResponse(llm_service.await_result(), media_type="text/event-stream")` 从 `chunk_list` 里逐个 `pop_chunk` 推给前端。

3. **run_task 内部步骤（顺序）**  
   - 若有数据源：  
     - **术语检索**：多轮时可结合历史问题做追问/指代词增强；`get_terminology_template_with_data` → 下发 step-start/step-complete（术语检索）。  
     - **训练数据检索**：`get_training_template_with_data`（embedding 相似 SQL 示例）→ 下发训练数据检索步骤事件。  
     - 若启用 xpack：**自定义提示词**（find_custom_prompts, GENERATE_SQL）。  
   - **select_datasource**：未绑定数据源时先选库（CHOOSE_DATASOURCE）。  
   - **generate_sql**：根据 schema、术语、训练数据、自定义提示词等组 prompt，调用 LLM 生成 SQL；可能走「直接 SQL」或「带子查询」分支；含权限模板（行/列）时走 GENERATE_SQL_WITH_PERMISSIONS。  
   - **check_sql / check_save_sql**：解析并校验 SQL，落库（save_sql 等）。  
   - **execute_sql**：对用户库执行 SQL，结果落库（save_sql_exec_data）。  
   - **generate_chart**：根据结果推荐/生成图表配置，可再调 LLM；check_save_chart、save_chart。  
   - **finish**：结束记录（finish_record），可能更新会话标题。

4. **消息与日志**  
   - 各步骤通过 `yield 'data:' + orjson.dumps({...}).decode() + '\n\n'` 推事件（type：step-start、step-complete、sql、chart、error、finish 等）。  
   - ChatLog 按操作类型（GENERATE_SQL、GENERATE_CHART、ANALYSIS、PREDICT_DATA 等）记录，供多轮上下文与审计。

### 4.3 关键文件

- **backend/apps/chat/task/llm.py**  
  - `LLMService` 类（体量大，约 2500+ 行）：  
    - 初始化：数据源、schema、术语、训练数据、LLM、会话日志、错误信息。  
    - 核心方法：`init_messages` / `init_straight_messages`、`generate_sql`、`execute_sql`、`generate_chart`、`run_task` / `run_task_cache` / `run_task_async`、`await_result` / `pop_chunk`。  
    - 其他：`select_datasource`、`generate_analysis`、`generate_predict`、`run_recommend_questions_task_*`、`run_analysis_or_predict_task_*`、权限/动态 SQL/子查询等。  
  - 依赖：chat curd（save_*、get_*）、template 各 generator、datasource（get_table_schema、permission）、terminology、data_training、ai_model（LLMFactory）、db（exec_sql）。

- **backend/apps/chat/api/chat.py**  
  - 路由：/list、/get/{chart_id}、/get/with_data、/record/.../data、/record/.../predict_data、/rename、/delete、/start、/assistant/start、/recommend_questions/{chat_record_id}、**/question**（流式问答）。

- **backend/apps/chat/curd/chat.py**  
  - 会话与记录的 CRUD、保存 SQL/图表/分析/预测结果、记录状态与日志查询。

- **backend/apps/chat/models/chat_model.py**  
  - Chat、ChatRecord、ChatQuestion、ChatLog、OperationEnum、ChatFinishStep、RenameChat、CreateChat 等。

---

## 五、数据与权限

- **多租户/工作区**：Chat 有 `oid`、`create_by`；数据源、术语、训练数据等按 `oid`/工作区隔离。  
- **数据源**：CoreDatasource 存连接信息；支持类型：PostgreSQL、MySQL、SQL Server、Oracle、Excel 等；表结构通过 db 层执行元数据查询或 Excel 解析得到。  
- **行/列权限**：datasource 的 permission/row_permission 等，在生成 SQL 时通过 template（filter/generator）注入条件。  
- **助手/嵌入式**：CurrentAssistant、origin=2、动态数据源（AssistantOutDs），可限制可见数据源与接口形态。

---

## 六、配置与环境

- **后端**：`common/core/config.py` 的 Settings（pydantic-settings），可从环境变量或指定 `.env` 读取（如 README 提到的 `backend/` 或文档中的 config 路径）。  
- **关键配置**：数据库 URL、CORS、TOKEN_KEY、ASSISTANT_TOKEN_KEY、缓存类型、日志、EMBEDDING_*、TABLE_EMBEDDING_* 等。  
- **前端**：`.env.development` / `.env.production`，请求基地址等。

---

## 七、与「项目分析」文档的对应关系

- **project_analysis.md** 中已总结：  
  - 功能完整、架构清晰；  
  - 缺点：LLMService 过大、职责可再拆、迁移文件多、前端状态与组件可优化、性能与体验可提升等。  
- 本文档侧重「当前定版下的结构与主流程」，便于：  
  - 快速回忆「一问一答」从请求到流式输出的路径；  
  - 定位 chat、datasource、template、ai_model、db、common 的职责；  
  - 后续若重构 LLMService 或改步骤顺序，有一张清晰的“地图”。

---

## 八、常用入口速查

| 目的 | 位置 |
|------|------|
| 修改问答主流程 / 步骤顺序 | backend/apps/chat/task/llm.py → run_task |
| 修改 SQL 生成 prompt | backend/apps/template/generate_sql/generator.py |
| 修改图表生成 prompt | backend/apps/template/generate_chart/generator.py |
| 术语检索 | backend/apps/terminology + chat/task/llm 中 get_terminology_template_with_data |
| 训练数据检索 | backend/apps/data_training + chat/task/llm 中 get_training_template_with_data |
| 数据源与表结构 | backend/apps/datasource/crud + apps/db |
| 聊天 API 与流式协议 | backend/apps/chat/api/chat.py + 前端 api/chat.ts、request.fetchStream |
| 会话/记录模型 | backend/apps/chat/models/chat_model.py |
| 前端问答页与流式展示 | frontend/src/views/chat/ |
| 路由与菜单 | frontend/src/router/index.ts |

---

以上为当前项目在「定版」基础上的整体分析，便于后续在此基础上做小步修改或渐进式重构时保持上下文一致。
