  我来结合你的 SQLBot 项目架构，为你定制一套完整的 Trae 重构需求方案。

---

## SQLBot 项目重构需求方案

### 一、项目现状诊断

基于你的架构分析，当前技术栈存在以下可现代化升级点：

| 维度 | 当前状态 | 现代化方向 |
|------|---------|-----------|
| **前端框架** | Vue 3.5 + Options API 混用 | 全面 Composition API + `<script setup>` 语法 |
| **状态管理** | Pinia 3.0（基础使用） | Pinia + 组合式函数（Composables）+ 状态持久化 |
| **后端架构** | FastAPI 0.115 + 传统分层 | FastAPI 0.115 + 依赖注入优化 + 异步架构强化 |
| **AI 集成** | LangChain 0.3 + LangGraph 0.3 | LangChain 0.3 + LangGraph 0.3（已较新，优化提示词工程） |
| **数据库** | SQLModel 0.0.21 | SQLModel 0.0.22+（支持 Pydantic V2）+ 连接池优化 |
| **前端构建** | Vite 6.3 | Vite 6.3（已较新，优化配置） |
| **可视化** | AntV G2 5.3 + S2 2.4 | 统一升级到 AntV 5.x 最新，探索 G2Plot 封装 |
| **类型安全** | TypeScript 5.7（基础配置） | 严格模式 + 类型体操（工具类型）+ API 契约共享 |

---

### 二、Trae 重构指令模板

#### **第一轮：架构分析与重构计划生成**

```markdown
## 任务：SQLBot 项目现代化重构分析

### 项目背景
- **源目录**：/cursor（现有 SQLBot 项目）
- **目标目录**：/re-sql（重构后代码存放位置）
- **项目类型**：智能问数系统（NL2SQL），前后端分离架构

### 当前技术栈
**后端**：
- Python 3.11 + FastAPI 0.115.12 + SQLModel 0.0.21
- LangChain 0.3 + LangGraph 0.3（对话图管理）
- PostgreSQL + pgvector + Redis 6.2
- Alembic 数据库迁移

**前端**：
- Vue 3.5.13 + TypeScript 5.7.2 + Vite 6.3.1
- Element Plus 2.10.1 + Pinia 3.0.2
- AntV G2 5.3.3 + S2 2.4.3 + X6 2.18.1
- Vue Router 4.5.0 + vue-i18n 9.14.4

### 需求 1：生成重构计划书
请分析 /cursor 目录结构，输出详细的《SQLBot 现代化重构计划书》，包含：

1. **模块优先级排序**（按依赖关系：基础工具 → 核心模块 → 业务组件）
2. **技术升级清单**（具体到版本号和迁移步骤）
3. **风险点识别**（如 LangGraph 状态管理、SQLModel 关系映射等）
4. **文件映射表**（旧文件路径 → 新文件路径对应关系）
5. **测试策略**（哪些模块需要单元测试、集成测试）

### 输出要求
- 生成 `/re-sql/REFACTOR_PLAN.md`
- 保持 /cursor 目录原样不动
- 使用 Markdown 表格展示关键决策
```

---

#### **第二轮：后端核心模块重构（以 Chat 模块为例）**

```markdown
## 任务：后端 Chat 模块现代化重构

### 重构范围
/cursor/backend/apps/chat/ → /re-sql/backend/apps/chat/

### 具体技术要求

#### 1. 架构模式升级
- **当前**：传统分层（router → service → model）
- **目标**：Clean Architecture / Ports and Adapters 模式
  - 定义清晰的接口边界（Protocols）
  - 依赖注入容器（使用 `dependency-injector` 或 FastAPI 原生 Depends）
  - 领域层（Domain）、应用层（Application）、基础设施层（Infrastructure）分离

#### 2. 异步与性能优化
- 所有 I/O 操作（DB、Redis、LLM API）必须真正的异步（`async/await`）
- 使用 `asyncpg` 替代 psycopg2（若尚未使用）
- 实现连接池管理（PostgreSQL 连接池、Redis 连接池）
- 添加请求耗时监控中间件（Prometheus 指标暴露）

#### 3. AI 层现代化（LangChain/LangGraph 优化）
- **提示词工程**：将硬编码 prompt 迁移到 `jinja2` 模板文件（`backend/prompts/`）
- **模型抽象**：定义 `LLMProvider` 协议，支持 OpenAI、Azure、Claude、本地模型无缝切换
- **对话状态管理**：使用 LangGraph 的持久化层（PostgresSaver/RedisSaver）替代内存存储
- **流式输出**：实现 SSE（Server-Sent Events）支持流式返回 SQL 生成过程

#### 4. 数据层优化
- **SQLModel 升级**：升级到 0.0.22+，全面使用 Pydantic V2 模型
- **查询优化**：复杂查询使用 `selectinload` 避免 N+1 问题
- **向量搜索**：pgvector 操作封装为独立 Repository 类

#### 5. 代码质量
- 类型提示覆盖率 100%（使用 `mypy --strict` 检查）
- 单元测试覆盖率 ≥ 80%（pytest + pytest-asyncio + httpx）
- 异常处理：自定义领域异常（DomainException）+ 全局异常处理器

### 关键文件重构清单
| 原文件 | 重构后 | 主要变更 |
|-------|--------|---------|
| question_enhance_llm.py | services/question_enhancement/llm_enhancer.py | 模板化、支持多模型 |
| question_enhancer.py | services/question_enhancement/rule_enhancer.py | 策略模式、规则引擎 |
| context_manager.py | infrastructure/persistence/context_store.py | LangGraph 持久化集成 |
| prompt_builder.py | domain/prompts/prompt_template.py | Jinja2 模板管理 |

### 输出要求
- 在 /re-sql/backend/apps/chat/ 生成完整重构代码
- 生成 `ARCHITECTURE.md` 说明新架构设计
- 生成 `pytest` 测试文件（tests/ 目录）
- 保持 API 接口契约不变（兼容前端）
```

---

#### **第三轮：前端核心模块重构（以 Chat 界面为例）**

```markdown
## 任务：前端 Chat 模块现代化重构

### 重构范围
/cursor/frontend/src/views/chat/ → /re-sql/frontend/src/views/chat/

### 具体技术要求

#### 1. 组合式 API 全面迁移
- **当前**：可能混用 Options API 和 Composition API
- **目标**：全面使用 `<script setup>` + Composition API
- 提取可复用逻辑为 Composables：
  - `useChat()`：聊天状态管理、消息发送
  - `useStreaming()`：SSE 流式响应处理
  - `useSQLHighlight()`：SQL 语法高亮

#### 2. 类型系统强化
- 定义完整的 API 契约类型（与后端共享或自动生成）
- 使用 `zod` 进行运行时数据校验（API 响应）
- 严格模式：`strict: true` + `noUncheckedIndexedAccess: true`

#### 3. 状态管理优化
- Pinia Store 按功能拆分：
  - `chatStore`：当前会话状态
  - `messageStore`：消息列表、分页
  - `contextStore`：对话上下文
- 使用 `pinia-plugin-persistedstate` 实现草稿自动保存

#### 4. 性能优化
- **虚拟列表**：消息列表使用 `vue-virtual-scroller` 处理长对话
- **懒加载**：路由级懒加载 + 组件异步加载
- **缓存策略**：使用 Vue Query (TanStack Query) 管理服务端状态缓存

#### 5. UI/UX 现代化
- **Element Plus 深度定制**：使用 CSS Variables 实现主题系统（支持暗黑模式）
- **消息组件重构**：
  - 支持 Markdown 渲染（代码块高亮、表格）
  - SQL 结果展示：可折叠的表格/图表切换
  - 消息操作：重新生成、复制、删除、分支对话
- **响应式优化**：移动端适配（触摸操作优化）

#### 6. 可视化升级
- 统一 AntV 图表封装：
  - 创建 `ChartRenderer` 组件（支持 G2、S2、X6）
  - 图表配置使用 JSON Schema 驱动
  - 支持图表导出（PNG/SVG/Excel）

### 关键组件重构清单
| 原组件 | 重构后 | 主要变更 |
|-------|--------|---------|
| ChatView.vue | views/chat/ChatContainer.vue | 布局优化、虚拟列表 |
| MessageList.vue | components/chat/MessageStream.vue | 流式渲染、分支管理 |
| MessageItem.vue | components/chat/MessageCard.vue | 富文本、代码高亮 |
| SQLPreview.vue | components/sql/SQLcursorBlock.vue | Monaco Editor 轻量版 |
| ChartView.vue | components/visualization/SmartChart.vue | Schema 驱动、多图表类型 |

### 输出要求
- 在 /re-sql/frontend/src/views/chat/ 生成完整重构代码
- 提取的 Composables 放在 `composables/chat/` 目录
- 生成组件文档（使用 Storybook 格式或 Markdown）
- 保持路由结构和 API 调用兼容
```

---

#### **第四轮：基础设施与工程化重构**

```markdown
## 任务：工程化与基础设施现代化

### 重构范围
根目录配置文件 → /re-sql/

### 具体要求

#### 1. 后端工程化
- **依赖管理**：使用 `uv` 或 `pdm` 替代 pip（现代 Python 包管理）
- **代码质量**：
  - Ruff（lint + format）替代 flake8/black
  - 提交前钩子：`pre-commit` 配置（ruff、mypy、pytest）
- **容器化**：
  - 多阶段构建 Dockerfile（生产镜像 < 200MB）
  - docker-compose.dev.yml（热重载）+ docker-compose.prod.yml
- **配置管理**：
  - 使用 `pydantic-settings` 管理环境变量
  - 区分 dev/test/prod 配置类

#### 2. 前端工程化
- **构建优化**：
  - Vite 插件：Visualizer（包分析）、Compression（gzip/brotli）
  - 按需加载：Element Plus、AntV 组件按需引入
- **代码规范**：
  - ESLint 9（flat config）+ `@antfu/eslint-config`
  - 提交信息规范：Commitlint + Conventional Commits
- **测试体系**：
  - Vitest（单元测试）+ Playwright（E2E 测试）
  - 覆盖率报告：v8 + CI 集成

#### 3. 数据库迁移策略
- Alembic 迁移脚本规范化命名（`YYYYMMDDHHMMSS_description.py`）
- 数据迁移：大表迁移使用 `pg-online-schema-change` 类工具思路
- 种子数据：初始化术语、示例数据源

#### 4. 文档与 API 契约
- **后端**：OpenAPI 3.1 规范（FastAPI 原生）+ Redoc 美化
- **前端**：API 类型自动生成（`openapi-typescript`）
- **共享**：若可能，使用 monorepo 结构共享类型定义

### 输出文件清单
```
/re-sql/
├── backend/
│   ├── pyproject.toml          # 现代 Python 项目配置
│   ├── uv.lock                 # 锁定依赖版本
│   ├── .pre-commit-config.yaml
│   └── Dockerfile
├── frontend/
│   ├── eslint.config.js        # ESLint 9 flat config
│   ├── vite.config.ts          # 优化配置
│   ├── playwright.config.ts
│   └── package.json            # 更新 scripts
├── docker-compose.yml
├── Makefile                    # 常用命令封装
└── README-REFACTOR.md          # 重构完成后的迁移指南
```

### 输出要求
- 生成所有配置文件（可直接使用）
- 提供从旧项目迁移的详细步骤（数据迁移、环境变量更新等）
```

---

### 三、向 Trae 提问的关键技巧

#### **分阶段执行策略**
根据 Trae 的上下文限制，建议按以下顺序分批处理：

1. **Day 1**：后端基础架构（models、core、config）
2. **Day 2**：后端业务模块（chat、datasource - 最复杂）
3. **Day 3**：前端基础（composables、utils、types）
4. **Day 4**：前端业务组件（views/chat、components）
5. **Day 5**：工程化配置、测试、文档

#### **每轮对话的固定结构**
```markdown
## 上下文
[简要说明这是第几轮重构，基于之前的什么成果]

## 本次任务
[具体模块和范围]

## 输入文件
[粘贴关键原代码片段，或说明文件路径]

## 特殊约束
- 必须兼容现有 API 契约
- 必须使用 async/await
- 必须包含类型提示
- [其他项目特定约束]

## 输出要求
- 代码存放路径：/re-sql/xxx
- 需要包含的测试/文档
- [其他要求]
```

#### **验证与迭代指令**
每轮重构后，使用以下指令让 Trae 自检：

```markdown
请检查刚才生成的代码：
1. 是否所有异步操作都使用了 async/await？
2. 类型提示是否完整（特别是函数返回值）？
3. 是否处理了可能的异常（try/except 或 Result 模式）？
4. 是否与上一轮重构的模块保持风格一致？
5. 列出任何需要我确认的设计决策
```

---

### 四、现代化技术亮点总结

重构后的 SQLBot 将具备以下技术优势：

| 特性 | 实现方式 | 业务价值 |
|------|---------|---------|
| **流式 SQL 生成** | SSE + LangGraph 流式节点 | 用户实时看到 AI 思考过程，体验提升 |
| **对话分支管理** | LangGraph 持久化 + 状态分叉 | 支持"从这个问题重新问"的交互 |
| **智能缓存** | Vue Query + 语义缓存 | 相同问题毫秒级响应，节省 Token |
| **多模型路由** | LLMProvider 协议 | 根据问题复杂度自动选择模型（省成本） |
| **主题系统** | CSS Variables + 动态切换 | 支持企业定制、暗黑模式 |
| **类型安全端到端** | Pydantic + TypeScript 共享 | 前后端契约一致，减少联调 Bug |

这套方案既保持了 SQLBot 的核心业务逻辑，又在架构层面实现了现代化，为未来的智能分析、多 Agent 协作等功能打下坚实基础。

 我来搜索一下 LangChain 1.x 的最新变化和迁移指南，为你提供准确的升级方案。
 基于 LangChain 1.0/1.1 的最新重大变更，我为你重新定制 SQLBot 的重构方案。这是从 0.3 到 1.x 的**架构级重构**，涉及核心 Agent 机制的根本改变。

---

## LangChain 1.x 核心变更对照表

| 特性 | LangChain 0.3 | LangChain 1.0/1.1 | 影响级别 |
|------|---------------|-------------------|----------|
| **Agent 构建** | `create_react_agent` (LangGraph) | `create_agent` (LangChain core) | 🔴 **架构级** |
| **执行器** | `AgentExecutor` | 完全移除，直接 `agent.invoke()` | 🔴 **架构级** |
| **Python 版本** | >=3.9 | **>=3.10** (3.9已弃用) | 🔴 **阻塞级** |
| **提示词** | `prompt` 参数 | `system_prompt` + middleware 动态提示 | 🟡 **API级** |
| **结构化输出** | 手动解析/JSON约束 | `response_format` 原生支持 TypedDict/Pydantic | 🟡 **API级** |
| **状态管理** | Pydantic State | TypedDict State (性能优化) | 🟡 **API级** |
| **包结构** | `langgraph.prebuilt` | `langchain.agents` | 🟢 **导入级** |
| **遗留代码** | `langchain.*` 混用 | 分离到 `langchain-classic` | 🟢 **导入级** |

---

## 针对 SQLBot 的 LangChain 1.x 重构策略

### 重构核心原则

SQLBot 的核心是**多轮对话 + NL2SQL + 上下文管理**，LangChain 1.x 的 `create_agent` + middleware 模式非常适合这种需求，但需要重新设计架构。

---

### 第一轮：LangChain 1.x 架构设计（关键！）

向 Trae 提出：

```markdown
## 任务：SQLBot LangChain 1.x 架构升级设计

### 背景
当前 SQLBot 使用 LangChain 0.3 + LangGraph 0.3，使用 `create_react_agent` 实现多轮对话和 SQL 生成。现在需要升级到 LangChain 1.1.x。

### 核心变更需求

#### 1. Agent 架构迁移（最关键）
从 `langgraph.prebuilt.create_react_agent` 迁移到 `langchain.agents.create_agent`：

**原架构（0.3）：**
- create_react_agent 管理 ReAct 循环
- AgentExecutor 执行
- Pydantic 定义 AgentState

**新架构（1.1）：**
- create_agent 直接创建（无需 Executor）
- Middleware 模式处理 pre/post hook
- TypedDict 定义状态（性能更好）
- 原生支持结构化输出（SQL 解析更可靠）

#### 2. SQLBot 专属 Middleware 设计
根据 1.x 的 middleware 系统，设计以下中间件：

| Middleware | 职责 | 对应原功能 |
|-----------|------|-----------|
| `ContextSummarizationMiddleware` | `before_model`：历史对话摘要 | 原 context_manager.py |
| `SQLValidationMiddleware` | `after_model`：SQL 语法校验 | 原 SQL 执行前的检查 |
| `SchemaInjectionMiddleware` | `before_model`：动态注入表结构 | 原 prompt_builder 的动态 schema |
| `HumanInTheLoopMiddleware` | `after_model`：危险 SQL 确认 | 原权限控制增强 |

#### 3. 结构化输出定义（替代原 SQL 解析）
定义 TypedDict 严格约束 LLM 输出：

```python
class SQLGenerationOutput(TypedDict):
    thought: str  # 思考过程
    sql: str      # 生成的 SQL
    parameters: List[Dict[str, Any]]  # 参数化查询
    is_safe: bool  # 是否安全（只读）
    confidence: float  # 置信度
    suggested_charts: Optional[List[str]]  # 推荐图表类型
```

#### 4. 工具函数迁移
原工具函数（@tool 装饰器）基本兼容，但需调整：
- 移除 `agent_scratchpad` 相关逻辑
- 工具错误处理移到 `wrap_tool_call` middleware
- 多工具并行调用优化（1.x 内置更高效）

### 输出要求
生成 `/re-sql/backend/core/agent/` 目录：
- `sql_agent.py`：create_agent 配置
- `middleware/`：上述四个 middleware 实现
- `schemas.py`：所有 TypedDict 输出定义
- `tools/`：迁移后的工具函数（execute_sql, get_schema 等）

### 约束
- 必须兼容现有 /cursor 的 API 契约（输入输出格式不变）
- Python 3.11 环境（符合 1.x 要求）
- 使用 langchain>=1.1.0, langgraph>=0.3（兼容 1.x）
```

---

### 第二轮：核心模块重构（Chat 模块）

```markdown
## 任务：Chat 模块 LangChain 1.x 重构

### 重构范围
/cursor/backend/apps/chat/ → /re-sql/backend/apps/chat/

### 具体文件映射

#### 1. question_enhance_llm.py → services/enhancement/llm_enhancer.py
**原实现（0.3）：**
- 直接调用 LLMChain
- 手动拼接提示词

**新实现（1.x）：**
- 使用 `create_agent` 的子调用（sub-agent）
- 定义结构化输出：
  ```python
  class EnhancedQuestionOutput(TypedDict):
      original_question: str
      enhanced_question: str
      missing_context: List[str]
      suggested_tables: List[str]
      confidence: float
  ```
- 通过 `response_format=EnhancedQuestionOutput` 获得结构化结果

#### 2. context_manager.py → middleware/context_middleware.py
**核心变更：**
- 实现 `ContextSummarizationMiddleware`
- 在 `before_model` 阶段：
  1. 检查消息长度
  2. 若超过阈值，调用 summarization sub-agent
  3. 将摘要注入 state

```python
class ContextSummarizationMiddleware:
    def before_model(self, state: AgentState, config: RunnableConfig):
        if len(state["messages"]) > 10:
            summary = self.summarize_agent.invoke({"messages": state["messages"][:5]})
            state["context_summary"] = summary["structured_response"]
        return state
```

#### 3. prompt_builder.py → middleware/schema_injection_middleware.py
**核心变更：**
- 原：手动构建 prompt 字符串
- 新：在 `before_model` 动态修改 `system_prompt`

```python
class SchemaInjectionMiddleware:
    def __init__(self, datasource_service):
        self.ds_service = datasource_service
    
    def before_model(self, state: AgentState, config: RunnableConfig):
        # 从 state 中提取提到的表名
        mentioned_tables = self.extract_tables(state["messages"][-1].content)
        schema = self.ds_service.get_schema(mentioned_tables)
        
        # 动态修改 system_prompt（1.x 支持）
        state["system_prompt"] = self.build_prompt_with_schema(schema)
        return state
```

#### 4. 新增：agent_factory.py（核心）
统一创建 SQLBot Agent：

```python
from langchain.agents import create_agent
from langchain_openai import ChatOpenAI

def create_sql_agent(
    model: str = "gpt-4o",
    tools: List = None,
    middlewares: List = None,
    response_format: Type[TypedDict] = SQLGenerationOutput
):
    return create_agent(
        model=model,
        tools=tools or default_tools(),
        system_prompt=BASE_SQL_SYSTEM_PROMPT,  # 静态部分
        response_format=response_format,
        middleware=middlewares or default_middlewares(),
    )
```

### 关键改进点
1. **移除 AgentExecutor**：直接 `agent.invoke()`，代码更简洁
2. **类型安全**：所有 LLM 输出都有 TypedDict 约束，无需手动解析 JSON
3. **流式支持**：1.x 的 `agent.stream()` 原生支持 SSE，无需额外封装
4. **错误处理**：工具错误在 `wrap_tool_call` 统一处理，不再分散

### 输出要求
- 生成完整代码到 /re-sql/backend/apps/chat/
- 包含 pytest 测试（测试 middleware 链）
- 生成 MIGRATION.md 记录 0.3→1.x 的 API 变化
```

---

### 第三轮：数据源与工具模块重构

```markdown
## 任务：数据源模块与工具函数 1.x 适配

### 重构范围
/cursor/backend/apps/datasource/tools/ → /re-sql/backend/apps/datasource/tools/

### 工具函数迁移清单

#### 1. execute_sql 工具
**适配点：**
- 保持 `@tool` 装饰器（1.x 完全兼容）
- 但错误处理改为抛出 ToolException，由 middleware 捕获

```python
from langchain_core.tools import tool, ToolException

@tool
def execute_sql(sql: str, datasource_id: int) -> Dict:
    """执行 SQL 查询，返回结果"""
    try:
        result = datasource_service.execute(sql)
        return {"success": True, "data": result}
    except Exception as e:
        # 1.x 推荐：抛出 ToolException 让 middleware 处理
        raise ToolException(f"SQL执行失败: {str(e)}")
```

#### 2. get_schema 工具
- 增加缓存（1.x 与 LangGraph 集成更好）
- 使用 `InMemoryCache` 或 `RedisCache`

#### 3. 新增：sql_validator 工具（配合 middleware）
独立工具用于 SQL 语法检查，不直接调用，供 middleware 使用。

### Middleware 实现：SQLValidationMiddleware

```python
from langchain.agents.middleware import after_model

class SQLValidationMiddleware:
    def after_model(self, state: AgentState, output: SQLGenerationOutput):
        # 1. 检查 SQL 语法
        if not self.validate_syntax(output["sql"]):
            # 触发重试或人工确认
            state["needs_correction"] = True
            state["error"] = "语法错误"
        
        # 2. 安全检查（只读）
        if not output["is_safe"]:
            # 触发 Human-in-the-loop
            state["requires_approval"] = True
            
        return state
```

### 输出要求
- /re-sql/backend/apps/datasource/tools/ 目录
- /re-sql/backend/core/agent/middleware/sql_validation.py
- 测试用例（模拟 ToolException 场景）
```

---

### 第四轮：LangGraph 状态持久化升级

```markdown
## 任务：对话状态持久化 1.x 重构

### 背景
原实现使用 LangGraph 的内存 Checkpoint 或自定义存储。1.x 与 LangGraph 深度整合，需适配新的持久化机制。

### 核心变更

#### 1. State 定义迁移
**原（0.3 Pydantic）：**
```python
class AgentState(BaseModel):
    messages: List[BaseMessage]
    context: Dict
```

**新（1.x TypedDict）：**
```python
from typing_extensions import TypedDict

class AgentState(TypedDict):
    messages: List[BaseMessage]
    context: Dict[str, Any]
    # 1.x 新增：结构化响应专用字段
    structured_response: Optional[SQLGenerationOutput]
```

#### 2. 持久化配置
使用 LangGraph 1.x 的 `PostgresSaver` / `RedisSaver`：

```python
from langgraph.checkpoint.postgres import PostgresSaver

# 替换原自定义的 context_manager 存储
checkpointer = PostgresSaver(conn=postgres_conn)

# 在 create_agent 中配置（通过 LangGraph 底层）
agent = create_agent(
    # ... 其他配置
    checkpointer=checkpointer,  # 自动持久化
)
```

#### 3. 对话分支管理（1.x 新特性）
利用 1.x 的 `interrupt` 机制实现"从这个问题重新问"：

```python
from langchain.agents.middleware import human_in_the_loop

# 在 middleware 中配置
InterruptOnConfig(
    condition=lambda state: state.get("requires_approval", False),
    message="该 SQL 涉及数据修改，是否确认执行？"
)
```

### 输出要求
- /re-sql/backend/core/persistence/ 目录
- 适配 PostgresSaver 的配置
- 对话恢复/分支管理的 API 实现
```

---

### 第五轮：前端适配与流式输出

```markdown
## 任务：前端 SSE 流式输出适配（1.x 新特性）

### 背景
LangChain 1.x 的 `agent.stream()` 产生的事件格式有变化，前端需适配。

### 关键变更

#### 1. 后端 SSE 实现（FastAPI）
```python
from fastapi import FastAPI
from fastapi.responses import StreamingResponse

@app.post("/chat/stream")
async def chat_stream(request: ChatRequest):
    agent = get_sql_agent()
    
    # 1.x 的 stream 产生结构化事件
    async def event_generator():
        async for event in agent.astream({"input": request.question}):
            # 事件格式变化：node 名从 "agent" 变为 "model"
            if event["node"] == "model":
                yield f"data: {json.dumps(event['output'])}\n\n"
            elif event["node"] == "tools":
                yield f"data: {json.dumps({'type': 'tool_call', 'tool': event['output']})}\n\n"
    
    return StreamingResponse(event_generator(), media_type="text/event-stream")
```

#### 2. 前端适配（Vue 3）
```typescript
// composables/useStreamingChat.ts
export function useStreamingChat() {
  const sendMessage = async (question: string) => {
    const response = await fetch('/chat/stream', {
      method: 'POST',
      body: JSON.stringify({ question })
    });
    
    const reader = response.body?.getReader();
    while (true) {
      const { done, value } = await reader!.read();
      if (done) break;
      
      const event = parseSSE(new TextDecursorr().decursor(value));
      
      // 1.x 新格式处理
      if (event.structured_response) {
        // 完整的结构化输出（SQL、图表推荐等）
        displaySQLResult(event.structured_response);
      } else if (event.content) {
        // 流式思考过程
        appendThinking(event.content);
      }
    }
  };
}
```

### 输出要求
- /re-sql/backend/apps/chat/router.py 新的 SSE 端点
- /re-sql/frontend/src/composables/useStreamingChat.ts
- 保持与原前端 API 的兼容性（非流式端点保留）
```

---

## 依赖升级清单（pyproject.toml）

```toml
[project]
requires-python = ">=3.11"  # 1.x 强制要求 3.10+，推荐 3.11

dependencies = [
    # LangChain 生态 - 1.x 升级
    "langchain>=1.1.0",           # 核心 1.x
    "langchain-core>=1.1.0",      # 核心接口
    "langchain-openai>=0.3.0",    # OpenAI 适配
    "langgraph>=0.3.0",           # 兼容 1.x
    
    # 注意：以下包已迁移或弃用
    # "langchain-community"  # 部分功能移到 langchain-classic
    
    # 若需遗留功能，显式安装
    # "langchain-classic>=1.0.0",
]
```

---

## 重构检查清单（给 Trae 的验证指令）

每轮重构后，向 Trae 确认：

```markdown
请验证以下 LangChain 1.x 合规性：

1. **Python 版本**：代码中无 Python 3.9 特性，使用 3.10+ 语法（如 `|` 类型联合）
2. **导入检查**：无 `from langgraph.prebuilt import create_react_agent`，应为 `from langchain.agents import create_agent`
3. **无 Executor**：代码中无 `AgentExecutor` 实例化
4. **TypedDict State**：所有 State 定义均为 TypedDict，非 Pydantic BaseModel
5. **Middleware 模式**：Pre/post hook 已实现为 middleware，而非直接传函数
6. **结构化输出**：SQL 生成等关键路径使用 `response_format` 参数
7. **工具错误**：工具函数使用 `ToolException` 而非直接返回错误字典
8. **流式事件**：若涉及流式，事件节点名称为 "model" 而非 "agent"

如有不符合，请修正。
```

---

这套方案充分利用了 LangChain 1.x 的 **Middleware 架构**、**原生结构化输出** 和 **简化 Agent 构建** 特性，将 SQLBot 的 NL2SQL 能力提升到更稳定、可扩展的新架构。